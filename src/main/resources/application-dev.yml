spring:
  datasource:
    url: jdbc:mysql://ec2-13-209-41-168.ap-northeast-2.compute.amazonaws.com:3307/boardmate_dev?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: devuser
    password: devpass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

server:
  port: 8080
